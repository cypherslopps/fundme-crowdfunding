import { BookDashed, Edit, Heart, Loader2, Plus } from "lucide-react";

const Icons = {
    plus: Plus,
    heart: Heart,
    loader: Loader2,
    edit: Edit,
    book: BookDashed
}

export default Icons;